{% extends 'base.html.twig' %}

{% block title %}TypeCours{% endblock %}

{% block body %}
    <h1>TypeCours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_cour.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ type_cour.intitule }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ type_cour.couleur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_cours_index') }}">back to list</a>

    <a href="{{ path('type_cours_edit', {'id': type_cour.id}) }}">edit</a>

    {{ include('type_cours/_delete_form.html.twig') }}
{% endblock %}
